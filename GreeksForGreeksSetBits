setBits(n)
    {
        //your code here
        
        let bainary= n.toString(2)
        let result=0

        
        for(let i=0; i<bainary.length; i++)
        {
            if(bainary[i]==1)
            {
                result++;
            }
        }

        return result
    }
