
function stringRotation(s,k){
let dif
    let result=s
    for(let i=0; i<s.length; i++)
    {
        dif= i-k
        if(dif<0)
        {
            result[s.length-dif]=s[i]
        }
        else result[dif]=s[i]
    }
    
    return result
    
    }
