/**
 * @param {string} s
 * @return {boolean}
 */
var isPalindrome = function(s) {
    s = s.replace(/\W/g, '');
    s= s.replace('_','')
    
    let reverse= s.split('').reverse().join('')
    if(s.toLowerCase()== reverse.toLowerCase()) return true
    else return false
}
